# Simulator intermediates: ~27 MB, regenerated byte-identically by
#   python analysis/prepare_weather.py && python analysis/simulate_courts.py
# Everything a reader needs without running anything -- metrics.json, the
# figures, the declared assumptions and the wait histogram -- is committed.
data/hourly_weather.csv
data/park_hours.csv
data/scenarios.csv

# rendered previews of the SVGs, for eyeballing only
outputs/figures/*.png
